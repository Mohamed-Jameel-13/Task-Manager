runtime: nodejs16

instance_class: F1

env_variables:
  NODE_ENV: "production"
  PORT: 8080

handlers:
  - url: /.*
    script: auto
    secure: always

automatic_scaling:
  min_idle_instances: 0
  max_idle_instances: 1
  min_pending_latency: 1000ms
  max_pending_latency: 3000ms
